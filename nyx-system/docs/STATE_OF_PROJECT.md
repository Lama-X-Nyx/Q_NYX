# STATE OF PROJECT — NYX

> **Read this FIRST.** Single source of truth for "what is actually true
> about this repo today". Every other doc is either (a) a drill-down on
> one topic or (b) historical. When in doubt, trust this file over any
> other prose in the repo.
>
> Last audited: branch `claude/run-pyright-system-qroCy`, 78 commits
> ahead of `main`.

The repo holds **several layers of history stacked on top of each
other** — base v0.8 → refonte v0.2.5 → pipeline unifié v0.3.1 →
hub-and-spoke multi-asset → paper-live infra → real-time live decider.
Docs accumulated as these layers landed and are not narratively fused.
This file separates them into **4 hard sections** so that future
sessions (human or AI) never again confuse "code present" with
"validated in stats" with "cible architecturale".

---

## 1. CURRENT PRODUCTION TRUTH

> What is **measured**, **tested**, and **used** right now.

### Engine (mono-asset, BTC reference)

The reference engine is **`NYXPipeline` v0.3.1** (file
`src/ml/nyx_pipeline.py`). It is the code path that produced every
validated number in section 2. It layers:

- MTF candidates (15m + 1h + 4h + 1d, 84 features total)
- ML filter (GradientBoostingClassifier, threshold 0.60)
- Conditional bear dial (stricter threshold + cooldown when regime
  hostile — ~40 % of bars in 2020-2022, less in bull 2023)
- Soft-gate sizing (ML confidence × rule agreement)
- Maker fees 0.0002 + slippage 0.0001
- Cooldown 32 bars / max_daily_trades = 1 / TP 1.5 × ATR / SL 1.0 × ATR

**Never consider the v0.8 or v0.2.5 code paths "production".** They are
history.

### Infrastructure (multi-asset, shared)

| Layer | Module | Tests GREEN |
|---|---|---:|
| Paper-live broker (post-only, no taker fallback) | `src/paper_live/post_only_broker.py` | all |
| Persistent decisions + missed-trade SQLite | `src/paper_live/persistent_decisions.py` | all |
| Atomic state + heartbeat | `src/paper_live/state_manager.py`, `heartbeat.py` | all |
| Telegram + Discord alerts | `src/paper_live/multi_alerter.py` | all |
| **Hub-and-spoke** (Pods → Allocator → Broker) | `src/assets/hub_spoke_runner.py` | all |
| Portfolio Allocator (risk caps, no-pyramid, cluster veto) | `src/assets/portfolio_allocator.py` | all |
| **Real-time ML decider** (per-bar 4-TF) | `src/ml/nyx_live_decider.py` | 15 |
| Rolling-window feature buffer | `src/ml/feature_buffer.py` | all |
| 4-TF aggregation stack | `src/ml/mtf_feature_stack.py` | all |

- Pyright: **0 errors** (`pyrightconfig.json`)
- `docs/OPERATING_RULES.md` Rules 1–7 enforced by test
  `tests/test_operating_rules.py`
- Full suite: **~265 tests GREEN**

### Models actually on disk (trained artefacts)

| Symbol | Artefact | Train end | # features | Validated OOS? |
|---|---|---|---:|---|
| **ETHUSDT** | `models/ETHUSDT/ml_filter_v1.pkl` + scaler + names | 2022-12-31 | 84 | 2023 OOS in §2 |
| **SOLUSDT** | `models/SOLUSDT/ml_filter_v1.pkl` + scaler + names | 2022-12-31 | 84 | 2023 OOS in §2 |

BTCUSDT has **no persisted artefact** in `models/`. The BTC numbers in
§2 come from training-inside-test-setup of `NYXPipeline.run()`, not a
loaded artefact. This is a gap vs ETH/SOL operational readiness.

### Known operational truths

- **Post-only miss rate on real data**: 14.6 % of ranked trades don't
  fill within `max_wait_bars` — they become `TIMED_OUT`, logged to
  `missed_trades.db`, never silently filled as taker.
- **`HubSpokeRunner` + `NYXPipelinePod` → 3.8 % allocator cut** when
  replaying A/B/C via the shared broker (was 98.5 % before the
  position-lifecycle fix — commits `b569ef2` → `eb29997`).
- **`NYXLiveDecider` + bear-dial conditional wire**: parity-tested
  against batch `NYXPipeline` on ETH H1 2023, directional agreement
  ≥ 60 % on overlapping timestamps, trade-count ratio 0.15 with
  documented cold-start asymmetry.

---

## 2. VALIDATED RESEARCH TRUTH

> Numbers with a clear code path + a test fixture. Honest means
> post-only fees, realistic slippage, daily-return Sharpe (not
> per-trade), and no B&H hindsight bias.

### A/B/C portfolio — 2022-2023 OOS (Monte-Carlo shuffle)

Source: `docs/ABC_BTC_ETH_SOL.md`, regenerated by
`scripts/validate_abc.py`.

| Portfolio | Assets | Trades | p5 Sharpe | p5 Return | prob_loss |
|---|---|---:|---:|---:|---:|
| A | BTC | 170 | 4.14 | +57 % | 0 % |
| B | BTC + ETH | 280 | 7.22 | +101 % | 0 % |
| **C** | **BTC + ETH + SOL** | **396** | **7.78** | **+126 %** | **0 %** |

### Walk-forward trio — 2020-2023 annualised

Source: `docs/WALK_FORWARD_TRIO.md`, `tests/test_walk_forward_trio.py`
(9/9 GREEN).

- 4/4 years positive
- **CAGR +49.3 %** (raw, pre-reality-check)
- Mean per-trade Sharpe 7.74 (annualised ×√N)
- Max annual DD 3.2 %

### Reality-check corrections (7 applied)

Source: `docs/REALITY_CHECK.md`,
`tests/test_reality_check_corrections.py`.

| Correction | Impact |
|---|---|
| 1. Post-only miss-rate 14.6 % | Edge still positive, lose ~27 % of gross PnL |
| 2. Taker-fee sensitivity | 4–10 % return loss in worst case, edge holds |
| 3. Sharpe daily-equity (not per-trade) | **4.64** (not 7.74) — the real comparable |
| 4. Monte-Carlo block bootstrap (not IID) | Wider CI but stays > 0 |
| 5. Pure-OOS SOL 2024-2026 (never seen) | Sharpe 7.25, +$4 296 on $10 k — generalises |
| 6. B&H benchmark hypocrisy fix (30 % forced-stop) | B&H with human stop = **−16 %**, not +1 283 %. Real alpha **+227 pp** |
| 7. Forced-stop B&H parity | Strategy +211 % vs B&H −16 % |

### NYXPipeline v0.3.1 BTC 4-year OOS

Source: `docs/PIPELINE_V031_RESULTS.md`, `docs/MC_FINAL_V031.md`.

| Year | Trades | WR | Sharpe | DD |
|---|---:|---:|---:|---:|
| 2020 | 57 | 51 % | +0.38 | 3.2 % |
| 2021 | 57 | 63 % | +3.05 | 2.4 % |
| 2022 | 83 | 59 % | +1.76 | 2.6 % |
| 2023 | 42 | 79 % | +5.18 | 0.6 % |
| **Total** | **239** | — | **+2.59 avg** | — |

Conditional bear dial vs static: Sharpe +0.22 in 2020, +0.52 in 2022
bear, -1.59 in 2023 bull. Net verdict: **use conditional** — DD
protection in bear > small return cost in bull.

### OOS live-replay (NYXLiveDecider, 2023 full year, honest)

Source: `scripts/oos_live_replay.py`,
`reports/OOS_live_replay_2023.json`.

| Asset | Trades | WR | Total Return | Max DD | PF |
|---|---:|---:|---:|---:|---:|
| ETHUSDT | 15 | 46.7 % | +0.51 % | 1.85 % | 1.12 |
| SOLUSDT | 1 | 100 % | +1.71 % | 0 % | ∞ |

These numbers are **much lower than batch NYXPipeline** (52+ trades on
ETH H1 alone) because the live path applies the bear dial correctly
during cold-start windows batch silently skips. **This is measured
honesty, not a regression** — see §3.

---

## 3. ARCHITECTURE READY BUT NOT YET VALIDATED END-TO-END

> Code exists, tests pass, **but the end-to-end "would a real live
> deployment on Binance produce these numbers" proof is not yet done.**

### What is built

- `NYXLiveDecider` (`src/ml/nyx_live_decider.py`) — real-time per-bar
  multi-TF decider. Loads persisted artefact, maintains 4 rolling
  buffers, applies hard gate → ML score → bear dial → cooldown.
- `NYXLivePod` + `NYXPipelinePod` — `SignalPod` wrappers for
  `HubSpokeRunner`.
- Position lifecycle: `Signal.expected_hold_bars` + timestamp-based
  release in `HubSpokeRunner._release_expired_positions`.
- `scripts/oos_live_replay.py` — drives the live decider bar-by-bar
  over 2023 and cross-checks outcomes against NYXPipeline via
  `_generate_candidates` (zero duplication).

### What is NOT yet done

| Gap | Consequence | File/Issue |
|---|---|---|
| **BTCUSDT has no persisted artefact** in `models/`. | Can't run the live decider for BTC — the best-validated asset. | missing `models/BTCUSDT/` |
| **Binance WebSocket adapter** not written. | No real-time bar feed into `NYXLiveDecider`. Everything live-tested is replay. | no `src/live/binance_ws.py` |
| **Live miss-rate vs paper miss-rate** never measured on real order books. | The 14.6 % paper number may be optimistic or pessimistic under real maker priority. | would need live Binance testnet |
| **Allocator live**: code ready (`PortfolioAllocator`), but with only ETH + SOL artefacts live, the 3-asset cluster cap logic is under-exercised. | Multi-asset live-ready status = **architecturally valid, operationally 2/3** | `docs/HUB_AND_SPOKE_ARCHITECTURE.md` explicitly says "activate only after ≥ 2 assets validated" — we have 2, but not live. |
| **XRPUSDT** listed as "next" in hub-spoke doc. | No artefact, no training metadata. Pure placeholder. | missing `models/XRPUSDT/` |

### Recommended activation order

1. Train + persist BTCUSDT artefact (reuse `train_asset_model.py`
   with `train_end='2022-12-31'` on BTC data).
2. Re-run `oos_live_replay.py` including BTC.
3. Write Binance WS adapter, feed into `NYXLivePod` on a testnet.
4. Measure real miss-rate for ≥ 1 month before enabling real capital.
5. Then flip `allocator_live = True` for the trio.

---

## 4. HISTORICAL / SUPERSEDED

> Docs you will find in the repo, but that are **not the current
> truth**. Do not let them anchor a session's understanding.

| Doc | Superseded by |
|---|---|
| `docs/README.md` v0.2.5 language, "200 tests" | This file + `PIPELINE_V031_RESULTS.md` + current test count (~265) |
| `docs/EVOLUTION_A_TO_B.md` | Historical context only — explains how v0.8 → v0.2.5 → v0.3.1 happened. Not a statement of current architecture. |
| `docs/PHASE1_BASELINE.md`, `docs/MTF_BASELINE.md` | Early exploration. Numbers here are pre-reality-check. |
| `docs/FRACTAL_*.md` (6 files) | Fractal-agent research stream — not integrated into `NYXPipeline` or the live decider. Parked. |
| `docs/HSMM_*.md` (3 files) | HSMM exploration — not wired. Parked. |
| `docs/SETUP_INVESTIGATION*.md` (5 files) | Iterative debugging of the "setup" rule block. Resolved; the current rule calc is in `src/ml/nyx_pipeline.py`. |
| `docs/SMC_*.md` (3 files) | Smart-money-concepts experiments. Not wired. Parked. |
| `docs/REGIME_*.md` (2 files) | Early regime-detection debugging. Superseded by `src/ml/conditional_dial.py` + `src/ml/bear_risk_dial.py`. |
| **5 Jesse agents** (`src/ml/jesse_agents.py`, 683 LOC, 45 tests) | Alternative modular RF architecture. **Not wired into production.** Kept as fallback. See `docs/JESSE_AGENTS_STATUS.md`. |

---

## Quick decision table for future sessions

| Question you want to answer | File to open |
|---|---|
| "What does the engine produce today?" | `src/ml/nyx_pipeline.py` + §1 above |
| "What OOS numbers can I quote?" | §2 above + `docs/PIPELINE_V031_RESULTS.md` + `docs/WALK_FORWARD_TRIO.md` |
| "Which assets have trained models?" | `models/` — ETH + SOL today |
| "Is the system live yet?" | **No.** §3 lists the 5 remaining gaps. |
| "Can I delete `src/ml/jesse_agents.py`?" | Only after a deliberate decision — see `docs/JESSE_AGENTS_STATUS.md`. |
| "Why is the README out of date?" | Layers of history. Trust this file. |

---

## Maintenance rule

Every commit that **changes the state of the project** (new validated
number, new asset trained, new live gap closed, new doc superseded)
must update the matching §1/§2/§3/§4 entry here. Reviewers must
refuse merges that break this invariant.
